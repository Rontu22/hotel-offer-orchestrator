node_modules/
dist/
.next/
out/
coverage/
*.tsbuildinfo
.DS_Store

# Secrets — never commit
.env
.env.*
!.env.example
!.env.aws.example
infra/.env.aws
**/*.pem
**/*.key

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
.terraform.lock.hcl
crash.log

# Editor/agent scaffolding. `next dev` regenerates frontend/AGENTS.md on every run;
# keeping it untracked stops it reappearing in a diff.
AGENTS.md
brag-output/

# Raw screen captures. Only the compressed docs/demo/demo.mp4 is committed; the
# originals are hundreds of MB and would blow past GitHub's 100MB file limit.
*.mov
*.MOV
