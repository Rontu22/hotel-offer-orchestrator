node_modules/
dist/
.next/
out/
coverage/
*.tsbuildinfo
.DS_Store

# Secrets — never commit
.env
.env.*
!.env.example
!.env.aws.example
infra/.env.aws
**/*.pem
**/*.key

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
.terraform.lock.hcl
crash.log

# Editor/agent scaffolding. `next dev` regenerates frontend/AGENTS.md on every run;
# keeping it untracked stops it reappearing in a diff.
AGENTS.md
brag-output/

# Screen captures stay local. The demo is hosted by GitHub as an attachment and
# linked from the README, so the repo carries no video at all.
docs/demo/
*.mov
*.MOV
